-- Hi, there!  Thanks for checking out my interview submission :)

-- FIRST start by running the backend .NET Core API project, via a VS Code terminal
-- navigate to the root of the /Dynatron folder
dotnet run --launch-profile https

-- NEXT launch the angular front end, via another VS Code terminal
-- navigate to /Dynatron/ui/customer-db
ng serve -o

-- NOW from the opened browser explore the app